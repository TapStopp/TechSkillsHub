# --- Database ---
# Local PostgreSQL connection string used by Prisma.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# --- App branding ---
NEXT_PUBLIC_APP_NAME="TechSkillsHub"
NEXT_PUBLIC_BRAND_NAME="Marist CSM TechSkillsHub"

# --- Suitable integration (mock for now) ---
SUITABLE_API_BASE_URL="https://api.suitable.co"
SUITABLE_API_KEY="replace_later"
SUITABLE_MOCK_MODE="true"

# --- Authentication (mock SSO for now) ---
SSO_MOCK_MODE="true"
